# Runtime dependencies for the whole repo (functions/ and api/ have their own
# deployment-scoped requirements files that must stay consistent with this one).
pyodbc>=5.0
pdfplumber>=0.11
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
azure-identity>=1.16
azure-storage-blob>=12.19
azure-search-documents>=11.4
azure-functions>=1.18
openai>=1.30
